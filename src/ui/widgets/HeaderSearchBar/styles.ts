import { createStyleSheet } from 'react-native-unistyles';

const stylesheet = createStyleSheet(theme => ({
  container: { paddingHorizontal: 14, paddingBottom: 14, paddingTop: 2, backgroundColor: 'white' }
}));

export default stylesheet;
